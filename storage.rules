rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    function isSignedIn() { return request.auth != null; }

    // Public images under images/ once uploaded.
    match /images/{allPaths=**} {
      allow read: if true;
      allow write: if isSignedIn();
    }
  }
}
